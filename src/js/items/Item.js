function Item(name, description, stats, weight) {
    'use strict';
    this.name = name;
    this.description = description;
    this.stats = stats;
    this.weight = weight;
}

module.exports = Item;
